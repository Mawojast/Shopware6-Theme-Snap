{% sw_extends '@Storefront/storefront/component/product/description.html.twig' %}

{% block component_product_description_title %}
    {% if theme_config('tsp-product-page-show-detail-description-title-switch') == 1 %}
        <h2 class="product-detail-description-title">
            {{ 'detail.descriptionTitle'|trans|sw_sanitize }} "{{ product.translated.name }}"
        </h2>
    {% endif %}
{% endblock %}